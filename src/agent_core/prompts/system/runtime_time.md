# 当前时间上下文

会话中的「当前时刻」由**每条用户消息正文前**的 **`[Time:YYYY-MM-DD HH:MM]`** 前缀标明（为该条用户消息发送时的本地时间，时区由 Agent 配置，通常为北京时间）。**系统提示不含实时时钟**，亦不在此重复动态时间。

推断「现在」或回答与时间相关的问题时，请以**最近一条用户消息**中的上述前缀为准（若存在）；较早消息中的前缀表示当时发送时刻，勿当作当前墙钟。
